# import python filefrom classes import *def main():    # Create a new workbook    workbook = Workbook()    workbook.add_sheet("Sheet1")    worksheet = workbook.get_sheet("Sheet1")    # Set values in cells    worksheet.set_cell_value(0, 0, 89)  # A1    worksheet.set_cell_value(0, 2, 8)  # C1    worksheet.set_cell_value(0, 3, "=A1 + 5")    worksheet.set_cell_value(0, 0, 1)  # A1  #  calculate_expression(worksheet, '1 + C1', 'B1')  # B1    # Print the values    print("Value of cell A1:", worksheet.get_cell_value(0, 0))    print("Value of cell C1 :", worksheet.get_cell_value(0, 2))    print("Value of cell D1 :", worksheet.get_cell_value(0, 3))    # Checking the dependencies - ***doesn't work*** #   worksheet.set_cell_value(0, 2, "=A1")  # C1  #  print("Value of cell C1 (should be 2):", worksheet.get_cell_value(0, 2))  #  print("Value of cell B1 (should be 3):", worksheet.get_cell_value(0, 1), "Error!!!!!!!")    # Test calculations with basic operations and custom functions    # calculate_expression(worksheet, '1 - 9', 'A3')    # print("Value of cell A3 (should be -8):", worksheet.get_cell_value(2, 0))    # calculate_expression(worksheet, 'sqrt(9)', 'F2')    # print("Value of cell F2 (should be 3):", worksheet.get_cell_value(1, 5))    # calculate_expression(worksheet, 'sqrt(9) + pow(2, 3)',    #                      'J1')  # When I put in only one argument to "pow" function I got an error    # print("Value of cell J1 (should be 11):", worksheet.get_cell_value(0, 9))    # calculate_expression(worksheet, 'A1 + A10', 'A5')    # print("Value of cell J1 (should be 89):", worksheet.get_cell_value(4, 0), "Error!!!!!!")    # calculate_expression(worksheet, 'min(2, 3) + A1 + max(7, 8, 9)',    #                      'B5')  # B5  #  print("Value of cell B5 (should be 100):", worksheet.get_cell_value(4, 1))#    # Attempting to access a cell that is not in the sheet -  ***doesn't work - returns an error***    #    print(worksheet.get_cell_value(0, 10))    # Expanding the worksheet  #if __name__ == "__main__":    main()